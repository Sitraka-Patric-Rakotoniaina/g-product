{{ form_start(form) }}
<div class="form-group">
    <h5>Catégories</h5>
    {{ form_row(form.categories) }}
</div>
<div class="row justify-content-md-center">
    <button type="submit" class="btn btn-secondary  btn-sm btn-block">Filtrer</button>
</div>
{{ form_end(form) }}
